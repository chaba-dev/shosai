# Version control

- Use Jujutsu (`jj`) for repository version-control operations, including status, diffs, logs, and commits.
- Use Git only when an external integration specifically requires a Git command.
